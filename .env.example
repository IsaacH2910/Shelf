# Optional local overrides for cloud access. Copy to .env and keep that file out of git.
# Settings → Cloud is the usual place to store these.

# SHELF_TUNNEL_HOSTNAME=shelf.example.com
# SHELF_TUNNEL_TOKEN=
